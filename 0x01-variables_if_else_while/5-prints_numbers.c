#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int LowNum;

	for (LowNum = 0; LowNum < 10; LowNum++)
		printf("%d", LowNum);
	printf("\n");
	return (0);
}
